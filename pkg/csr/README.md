# The `csr` provides handy functions to validate a CSR if or not a "ManagedClusterCSR", authorize a CSR, and approve a CSR, etc.

# The `csr` is expected be used in a controller which watches the CSR resources.